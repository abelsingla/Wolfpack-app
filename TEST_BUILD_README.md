# Wolfpack Campaign Log Test Build

This folder contains the static production build for testing.

## Quick Test

Use any simple static server from this folder. For example:

```bash
python3 -m http.server 8080
```

Then open:

```text
http://localhost:8080
```

The app saves campaign data in the browser local storage. Use the download/upload buttons in the header to export or import campaign JSON.
